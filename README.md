# cockpit-test-source

Test repository (personal account: `PierPanda`).

Used to validate Cockpit cross-org operations:
- Create PRs from `main` to `production`
- Cross-org push / cherry-pick to `sakuga-client-test/cockpit-test-client`
