# cockpit-test-client

Test repository simulating a **client** project (org: `sakuga-client-test`).

Used to validate Cockpit cross-org operations:
- Receive cherry-picks / pushes from `sakuga-software/cockpit-test-source`
- `production` branch is deployed via Vercel
